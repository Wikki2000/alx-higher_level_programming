#!/usr/bin/node
exports.add = function (a, b) {
  const sum = Number(a) + Number(b);
#!/usr/bin/node
function add (a, b) {
  const sum = Number(a) + Number(b);
  return sum;
}
exports.add = function;
  return sum;
};
